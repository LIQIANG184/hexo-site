---
title: "[开发工具]--一个可以测试所有网络请求的网站"
date: 2026-03-24 14:48:24
tags:
  - 笔记
categories:
  - "工具分享"
from_note: true
source_note: "工具分享/[开发工具]--一个可以测试所有网络请求的网站.md"
---

 ----网站地址------  https://httpbin.org/ 

    DESCRIPTION
    Testing an HTTP Library can become difficult sometimes.RequestBin is fantastic 
    for testing POST requests, but doesn't let you control the response.This exists 
    to cover all kinds of HTTP scenarios. Additional endpoints are being considered.
<!-- more -->

**一** 使用条件: 

1 有一定的网络基础知识,
2 能够明白网址,请求等基础术语
3 能够自己拼接网络地址
4 明白网址结构构成
5 有至少一种编程语言基础

**二** 使用情景: 
1  当你需要急需一个网址进行get/post等请求方法的测试的时候
2  获取本机原始ip
3  获取UUID4
4 获取本次网络请求的user-agent

**三** 使用方法: https://httpbin.org/ 按照网站的内容后边直接拼接对应的路径

**四** 部分内容截图
![网站内容部分截图.png](/images/note/a4299525f3da41ea3cbfcc5c3f62ed68.webp)

例如:

   1 GET请求:

        1.1 访问地址:https://httpbin.org/get
        1.2 请求方法: GET
        1.3 返回内容:
              {
              "args": {},
              "headers": {
                  "Accept": "text/html,application/xhtml+xml,application/xml;q=0.9,image/webp,image/apng,*/*;q=0.8",
                  "Accept-Encoding": "gzip, deflate, br",
                  "Accept-Language": "zh-CN,zh;q=0.9,en;q=0.8",
                  "Connection": "close",
                  "Cookie": "_gauges_unique_year=1; _gauges_unique=1; _gauges_unique_hour=1; _gauges_unique_day=1; _gauges_unique_month=1",
                  "Host": "httpbin.org",
                  "Upgrade-Insecure-Requests": "1",
                  "User-Agent": "Mozilla/5.0 (Macintosh; Intel Mac OS X 10_12_6) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/63.0.3239.84 Safari/537.36"
              },
              "origin": "111.206.175.44",
              "url": "https://httpbin.org/get"
          }
  2 POST请求 

       2.1 访问地址:https://httpbin.org/post
       2.2 请求方法: POST
   
 当然还有很多请求,详见附录:
     
   *   [`/`](https://httpbin.org/)
    This page.
*   [`/ip`](https://httpbin.org/ip)

    Returns Origin IP.
*   [`/uuid`](https://httpbin.org/uuid)

    Returns UUID4.
*   [`/user-agent`](https://httpbin.org/user-agent)

    Returns user-agent.
*   [`/headers`](https://httpbin.org/headers)

    Returns header dict.
*   [`/get`](https://httpbin.org/get)

    Returns GET data.
*   `/post`

    Returns POST data.
*   `/patch`

    Returns PATCH data.
*   `/put`

    Returns PUT data.
*   `/delete`

    Returns DELETE data
*   [`/anything`](https://httpbin.org/anything)

    Returns request data, including method used.
*   `/anything/:anything`

    Returns request data, including the URL.
*   [`/encoding/utf8`](https://httpbin.org/encoding/utf8)

    Returns page containing UTF-8 data.
*   [`/gzip`](https://httpbin.org/gzip)

    Returns gzip-encoded data.
*   [`/deflate`](https://httpbin.org/deflate)

    Returns deflate-encoded data.
*   [`/brotli`](https://httpbin.org/brotli)

    Returns brotli-encoded data.
*   [`/status/:code`](https://httpbin.org/status/418)

    Returns given HTTP Status code.
*   [`/response-headers?key=val`](https://httpbin.org/response-headers?Server=httpbin&Content-Type=text%2Fplain%3B+charset%3DUTF-8)

    Returns given response headers.
*   [`/redirect/:n`](https://httpbin.org/redirect/6)

    302 Redirects

    *n*

    times.
*   [`/redirect-to?url=foo`](https://httpbin.org/redirect-to?url=http%3A%2F%2Fexample.com%2F)

    302 Redirects to the

    *foo*

    URL.
*   [`/redirect-to?url=foo&status_code=307`](https://httpbin.org/redirect-to?url=http%3A%2F%2Fexample.com%2F&status_code=307)

    307 Redirects to the

    *foo*

    URL.
*   [`/relative-redirect/:n`](https://httpbin.org/relative-redirect/6)

    302 Relative redirects

    *n*

    times.
*   [`/absolute-redirect/:n`](https://httpbin.org/absolute-redirect/6)

    302 Absolute redirects

    *n*

    times.
*   [`/cookies`](https://httpbin.org/cookies)

    Returns cookie data.
*   [`/cookies/set?name=value`](https://httpbin.org/cookies/set?k1=v1&k2=v2)

    Sets one or more simple cookies.
*   [`/cookies/delete?name`](https://httpbin.org/cookies/delete?k1=&k2=)

    Deletes one or more simple cookies.
*   [`/basic-auth/:user/:passwd`](https://httpbin.org/basic-auth/user/passwd)

    Challenges HTTPBasic Auth.
*   [`/hidden-basic-auth/:user/:passwd`](https://httpbin.org/hidden-basic-auth/user/passwd)

    404'd BasicAuth.
*   [`/digest-auth/:qop/:user/:passwd/:algorithm`](https://httpbin.org/digest-auth/auth/user/passwd/MD5/never)

    Challenges HTTP Digest Auth.
*   [`/digest-auth/:qop/:user/:passwd`](https://httpbin.org/digest-auth/auth/user/passwd/MD5/never)

    Challenges HTTP Digest Auth.
*   [`/stream/:n`](https://httpbin.org/stream/20)

    Streams

    *min(n, 100)*

    lines.
*   [`/delay/:n`](https://httpbin.org/delay/3)

    Delays responding for

    *min(n, 10)*

    seconds.
*   [`/drip?numbytes=n&duration=s&delay=s&code=code`](https://httpbin.org/drip?code=200&duration=5&numbytes=5)

    Drips data over a duration after an optional initial delay, then (optionally) returns with the given status code.
*   [`/range/1024?duration=s&chunk_size=code`](https://httpbin.org/range/1024)

    Streams

    *n*

    bytes, and allows specifying a

    *Range*

    header to select a subset of the data. Accepts a

    *chunk_size*

    and request

    *duration*parameter.
*   [`/html`](https://httpbin.org/html)

    Renders an HTML Page.
*   [`/robots.txt`](https://httpbin.org/robots.txt)

    Returns some robots.txt rules.
*   [`/deny`](https://httpbin.org/deny)

    Denied by robots.txt file.
*   [`/cache`](https://httpbin.org/cache)

    Returns 200 unless an If-Modified-Since or If-None-Match header is provided, when it returns a 304.
*   [`/etag/:etag`](https://httpbin.org/etag/etag)

    Assumes the resource has the given etag and responds to If-None-Match header with a 200 or 304 and If-Match with a 200 or 412 as appropriate.
*   [`/cache/:n`](https://httpbin.org/cache/60)

    Sets a Cache-Control header for

    *n*

    seconds.
*   [`/bytes/:n`](https://httpbin.org/bytes/1024)

    Generates

    *n*

    random bytes of binary data, accepts optional

    *seed*

    integer parameter.
*   [`/stream-bytes/:n`](https://httpbin.org/stream-bytes/1024)

    Streams

    *n*

    random bytes of binary data in chunked encoding, accepts optional

    *seed*

    and

    *chunk_size*integer parameters.
*   [`/links/:n`](https://httpbin.org/links/10)

    Returns page containing

    *n*

    HTML links.
*   [`/image`](https://httpbin.org/image)

    Returns page containing an image based on sent Accept header.
*   [`/image/png`](https://httpbin.org/image/png)

    Returns a PNG image.
*   [`/image/jpeg`](https://httpbin.org/image/jpeg)

    Returns a JPEG image.
*   [`/image/webp`](https://httpbin.org/image/webp)

    Returns a WEBP image.
*   [`/image/svg`](https://httpbin.org/image/svg)

    Returns a SVG image.
*   [`/forms/post`](https://httpbin.org/forms/post)

    HTML form that submits to

    */post*
*   [`/xml`](https://httpbin.org/xml)

    Returns some XML

